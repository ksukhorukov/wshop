<%= form_for purchase do |f| %>
	<%= f.label :email %>
	<%= f.text_field :email %>
	<%= f.label :card %>
	<%= f.text_field :card_truncated %>
	<%= f.hidden_field :cart_id %>
	<%= f.submit %>
<% end %>